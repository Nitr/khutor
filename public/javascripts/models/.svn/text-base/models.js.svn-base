	var trackerModel = Backbone.Model.extend({
		urlRoot: 'http://localhost/model'
	});
	var traker = Backbone.Model.extend({
		urlRoot: 'http://localhost/main'
	});
	var trackerData = Backbone.Model.extend({
		urlRoot: 'http://localhost/tracker'
	});

